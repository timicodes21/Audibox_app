export type LoginFormValues = {
  email: string;
  password: string;
};

export type SignupFormValues = {
  userName: string;
  email: string;
  password: string;
};

export type VerificationFormValues = {
  code: string;
};
